# Client — copy to .env (gitignored)

# Local dev — leave empty to use Vite proxy (/api -> localhost:5000)
VITE_API_URL=

# Vercel production (epicurean-havens project → Settings → Environment Variables):
# VITE_API_URL=https://epicurean-havens-server.vercel.app
