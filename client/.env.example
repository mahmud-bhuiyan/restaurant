# Client — copy to .env (gitignored)
# Leave VITE_API_URL empty in dev to use Vite proxy (/api -> localhost:5000)
VITE_API_URL=

# Production (separate host):
# VITE_API_URL=https://api.epicureanhaven.com
